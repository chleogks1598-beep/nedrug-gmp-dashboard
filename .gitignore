node_modules/
newdocs/
public/data.bak.json
.vercel
.DS_Store
*.log
# ephemeral working files (generated in CI, not committed)
change-orders.current.json
co-email-body.html
email-body.html
email-payload.json
gmp-pending-email.html
safety-email-body.html
prev-data.json
prev-recall.json
prev-admin.json
# pending/ 자체는 의도적으로 커밋한다 (수집 단계 → 추출 단계 인수인계용).
# 추출 중간산출물만 제외.
pending/extracted.json
# 클라우드 루틴 프롬프트 원본 백업 (진단용으로 프롬프트를 바꿀 때 원복 기준)
.routine-backup/

.vercel

# 로컬 자동갱신 러너 로그
local-update.log
# 보존본 다운로드 실패 목록 — 다음 실행이 재시도용으로 쓰는 로컬 작업파일
archive-failed.json
# pull 이 막힐 때 원격과 내용이 다른 untracked 파일을 대피시키는 곳 (scripts/git-sync.mjs)
.conflict/
